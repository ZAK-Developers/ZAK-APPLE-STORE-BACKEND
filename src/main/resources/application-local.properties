## Local development profile (used by default)
## Override these values in your machine as needed.

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/ZAKapplestoreDB
spring.datasource.username=postgres
spring.datasource.password=Reshma@786
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=true

# JWT Configuration
jwt.secret=ZakAppleStoreSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLongForSecurity
jwt.expiration=86400000

# Local bootstrap admin
bootstrap.admin.email=admin@zakapplestore.com
bootstrap.admin.password=admin123
bootstrap.admin.full-name=Store Admin
